/**
 * Note: This endpoint does not accept a request body.
 * Refresh token is passed through cookie (refreshToken).
 *
 * For response documentation see RefreshTokenResponseDto in response.dto.ts
 *
 * Usage example:
 * POST /auth/refresh
 * Cookies: refreshToken=your_refresh_token_here
 */
